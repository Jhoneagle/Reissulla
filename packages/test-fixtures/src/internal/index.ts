/**
 * Reissulla's own API response shapes — for FE component tests that mount
 * real query hooks against MSW.
 *
 * Populated lazily as FE specs are migrated; the empty barrel keeps the
 * package importable from the start.
 */

export {};
