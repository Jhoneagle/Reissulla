export function MapPage() {
  return (
    <section aria-labelledby="map-heading">
      <h2 id="map-heading">Map</h2>
      <p>Explore locations, weather, and transit on the map.</p>
    </section>
  );
}
