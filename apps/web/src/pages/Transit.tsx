export function Transit() {
  return (
    <section aria-labelledby="transit-heading">
      <h2 id="transit-heading">Transit</h2>
      <p>Search for stops, departures, and plan routes.</p>
    </section>
  );
}
