export function Dashboard() {
  return (
    <section aria-labelledby="dashboard-heading">
      <h2 id="dashboard-heading">Dashboard</h2>
      <p>Your weather and transit overview at a glance.</p>
    </section>
  );
}
